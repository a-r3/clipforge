"""
Subpackage for CLI subcommands.

Each command is implemented as a module with a `main` function that accepts an
optional list of arguments. The main function is invoked by the top‑level
argument parser defined in `autovideo/cli.py`.
"""
